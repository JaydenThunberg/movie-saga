import React, { Component } from 'react';

class _template extends Component {
  // Renders the entire _template on the DOM
  render() {
    return (
      <div className="_template">
        <p>_template</p>
      </div>
    );
  }
}

export default _template;